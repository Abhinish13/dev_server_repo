#!/bin/bash

PACKAGES=$1

for i in $(apt-cache depends ${PACKAGES} | grep -E 'Depends|Recommends|Suggests' | cut -d ':' -f 2,3 | sed -e s/'<'/''/ -e s/'>'/''/); do sudo apt-get download $i 2>>errors.txt; done
